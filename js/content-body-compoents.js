let contentNavLis = document.querySelectorAll('.content-nav li');

let slideLeft = document.querySelector('.slideLeft i');

let slideRight = document.querySelector('.slideRight i');

let slideShowBox = document.querySelector('.slideShowBox ul');

let slidePoints = document.querySelectorAll('.slidePoints a');

export {
    contentNavLis,
    slideLeft,
    slideRight,
    slideShowBox,
    slidePoints
}
