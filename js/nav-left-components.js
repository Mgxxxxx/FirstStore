
let createdList = document.querySelector('.created-list');

let createListBtn = document.querySelector('.nav-sub-head i');

let createListToggleBtn = document.querySelectorAll('.nav-sub-head i')[1];

let collectedListToggleBtn = document.querySelectorAll('.nav-sub-head i')[2];

let navLeftBody = document.querySelector('.nav-left-body');

let collectedList = document.querySelector('.collected-list');

let scrollBar = document.querySelector('.scroll-bar');

export {
    createdList,
    createListBtn,
    createListToggleBtn,
    collectedListToggleBtn,
    navLeftBody,
    collectedList,
    scrollBar
};
