let progress = document.querySelector('.playing-mess progress');

let aboutMusic = document.querySelector('.about-music');

let control = document.querySelector('.control');

let moreInfo = document.querySelector('.more-info');

let audio = document.querySelector('audio');

let volControl = document.querySelector('.vol-control input[type=range]');

let volControlIcon = document.querySelector('.vol-control .vol-control-bottom').children[0];

export {
    progress,
    aboutMusic,
    control,
    moreInfo,
    audio,
    volControl,
    volControlIcon
}
